// Kannada-English Translator for SkillServe

// Common translations for the application
const translations = {

    'home': { kannada: 'ಮನೆ', english: 'Home' },
    'dashboard': { kannada: 'ಡ್ಯಾಶ್‌ಬೋರ್ಡ್', english: 'Dashboard' },
    'profile': { kannada: 'ಪ್ರೊಫೈಲ್', english: 'Profile' },
    'logout': { kannada: 'ಲಾಗ್ ಔಟ್', english: 'Logout' },
    'login': { kannada: 'ಲಾಗಿನ್', english: 'Login' },
    'signup': { kannada: 'ಸೈನ್ ಅಪ್', english: 'Sign Up' },
    

    'electrician': { kannada: 'ವಿದ್ಯುತ್ ಕೆಲಸಗಾರ', english: 'Electrician' },
    'plumber': { kannada: 'ಕೊಳವೆ ಕೆಲಸಗಾರ', english: 'Plumber' },
    'painter': { kannada: 'ಚಿತ್ರಕಾರ', english: 'Painter' },
    'carpenter': { kannada: 'ಬಡಗಿ', english: 'Carpenter' },
    'mechanic': { kannada: 'ಮೆಕ್ಯಾನಿಕ್', english: 'Mechanic' },
    'cleaner': { kannada: 'ಸ್ವಚ್ಛತಾ ಕೆಲಸಗಾರ', english: 'Cleaner' },
    
   
    'available': { kannada: 'ಲಭ್ಯ', english: 'Available' },
    'busy': { kannada: 'ಕಾರ್ಯನಿರತ', english: 'Busy' },
    'book now': { kannada: 'ಈಗ ಬುಕ್ ಮಾಡಿ', english: 'Book Now' },
    'call now': { kannada: 'ಈಗ ಕರೆ ಮಾಡಿ', english: 'Call Now' },
    'contact': { kannada: 'ಸಂಪರ್ಕ', english: 'Contact' },
    'location': { kannada: 'ಸ್ಥಳ', english: 'Location' },
    'experience': { kannada: 'ಅನುಭವ', english: 'Experience' },
    'rating': { kannada: 'ರೇಟಿಂಗ್', english: 'Rating' },
    'description': { kannada: 'ವಿವರಣೆ', english: 'Description' },
    
    // Booking related
    'my bookings': { kannada: 'ನನ್ನ ಬುಕಿಂಗ್‌ಗಳು', english: 'My Bookings' },
    'pending': { kannada: 'ಬಾಕಿ ಇರುವ', english: 'Pending' },
    'accepted': { kannada: 'ಸ್ವೀಕರಿಸಲಾಗಿದೆ', english: 'Accepted' },
    'completed': { kannada: 'ಪೂರ್ಣಗೊಂಡಿದೆ', english: 'Completed' },
    'cancelled': { kannada: 'ರದ್ದುಗೊಳಿಸಲಾಗಿದೆ', english: 'Cancelled' },
    'rejected': { kannada: 'ತಿರಸ್ಕರಿಸಲಾಗಿದೆ', english: 'Rejected' },
    

    'name': { kannada: 'ಹೆಸರು', english: 'Name' },
    'email': { kannada: 'ಇಮೇಲ್', english: 'Email' },
    'password': { kannada: 'ಪಾಸ್‌ವರ್ಡ್', english: 'Password' },
    'phone': { kannada: 'ಫೋನ್', english: 'Phone' },
    'address': { kannada: 'ವಿಳಾಸ', english: 'Address' },
    'save': { kannada: 'ಉಳಿಸಿ', english: 'Save' },
    'cancel': { kannada: 'ರದ್ದುಮಾಡಿ', english: 'Cancel' },
    'edit': { kannada: 'ಸಂಪಾದಿಸಿ', english: 'Edit' },
    'delete': { kannada: 'ಅಳಿಸಿ', english: 'Delete' },
    'search': { kannada: 'ಹುಡುಕಿ', english: 'Search' },
    'filter': { kannada: 'ಫಿಲ್ಟರ್', english: 'Filter' },
    'clear': { kannada: 'ತೆರವುಗೊಳಿಸಿ', english: 'Clear' },
    
    'welcome': { kannada: 'ಸ್ವಾಗತ', english: 'Welcome' },
    'thank you': { kannada: 'ಧನ್ಯವಾದಗಳು', english: 'Thank You' },
    'please wait': { kannada: 'ದಯವಿಟ್ಟು ಕಾಯಿರಿ', english: 'Please Wait' },
    'loading': { kannada: 'ಲೋಡ್ ಆಗುತ್ತಿದೆ', english: 'Loading' },
    'success': { kannada: 'ಯಶಸ್ವಿ', english: 'Success' },
    'error': { kannada: 'ದೋಷ', english: 'Error' },
    
    'find local skilled workers': { kannada: 'ಸ್ಥಳೀಯ ನುರಿತ ಕೆಲಸಗಾರರನ್ನು ಹುಡುಕಿ', english: 'Find Local Skilled Workers' },
    'book service': { kannada: 'ಸೇವೆಯನ್ನು ಬುಕ್ ಮಾಡಿ', english: 'Book Service' },
    'describe your issue': { kannada: 'ನಿಮ್ಮ ಸಮಸ್ಯೆಯನ್ನು ವಿವರಿಸಿ', english: 'Describe Your Issue' },
    'preferred date': { kannada: 'ಆದ್ಯತೆಯ ದಿನಾಂಕ', english: 'Preferred Date' },
    'preferred time': { kannada: 'ಆದ್ಯತೆಯ ಸಮಯ', english: 'Preferred Time' },
    'contact number': { kannada: 'ಸಂಪರ್ಕ ಸಂಖ್ಯೆ', english: 'Contact Number' },
    'years of experience': { kannada: 'ವರ್ಷಗಳ ಅನುಭವ', english: 'Years of Experience' },
    'rate your experience': { kannada: 'ನಿಮ್ಮ ಅನುಭವವನ್ನು ರೇಟ್ ಮಾಡಿ', english: 'Rate Your Experience' },
    'submit rating': { kannada: 'ರೇಟಿಂಗ್ ಸಲ್ಲಿಸಿ', english: 'Submit Rating' },
    'mark as completed': { kannada: 'ಪೂರ್ಣಗೊಂಡಿದೆ ಎಂದು ಗುರುತಿಸಿ', english: 'Mark as Completed' },
    'mark as available': { kannada: 'ಲಭ್ಯ ಎಂದು ಗುರುತಿಸಿ', english: 'Mark as Available' },
    'mark as busy': { kannada: 'ಕಾರ್ಯನಿರತ ಎಂದು ಗುರುತಿಸಿ', english: 'Mark as Busy' }
};

// Current language state
let currentLanguage = localStorage.getItem('skillserve_language') || 'english';

// Initialize translator
function initializeTranslator() {
    // Add language toggle button to navigation
    addLanguageToggle();
    
    // Apply current language
    applyLanguage(currentLanguage);
}

// Add language toggle button to navigation
function addLanguageToggle() {
    const navElements = document.querySelectorAll('.nav');
    
    navElements.forEach(nav => {
        // Check if toggle already exists
        if (nav.querySelector('.language-toggle')) return;
        
        const languageToggle = document.createElement('button');
        languageToggle.className = 'btn language-toggle';
        languageToggle.innerHTML = currentLanguage === 'english' ? 'ಕನ್ನಡ' : 'English';
        languageToggle.title = currentLanguage === 'english' ? 'Switch to Kannada' : 'Switch to English';
        languageToggle.onclick = toggleLanguage;
        
        // Insert before logout link
        const logoutLink = nav.querySelector('[onclick="logout()"]');
        if (logoutLink) {
            nav.insertBefore(languageToggle, logoutLink);
        } else {
            nav.appendChild(languageToggle);
        }
    });
}

// Toggle between languages
function toggleLanguage() {
    currentLanguage = currentLanguage === 'english' ? 'kannada' : 'english';
    localStorage.setItem('skillserve_language', currentLanguage);
    
    // Update toggle button text
    const toggleButtons = document.querySelectorAll('.language-toggle');
    toggleButtons.forEach(btn => {
        btn.innerHTML = currentLanguage === 'english' ? 'ಕನ್ನಡ' : 'English';
        btn.title = currentLanguage === 'english' ? 'Switch to Kannada' : 'Switch to English';
    });
    
    // Apply language to page
    applyLanguage(currentLanguage);
}

// Apply language to the page
function applyLanguage(language) {
    // Translate elements with data-translate attribute
    const translatableElements = document.querySelectorAll('[data-translate]');
    
    translatableElements.forEach(element => {
        const key = element.getAttribute('data-translate').toLowerCase();
        if (translations[key] && translations[key][language]) {
            if (element.tagName === 'INPUT' && element.type === 'submit') {
                element.value = translations[key][language];
            } else if (element.tagName === 'INPUT' && element.placeholder) {
                element.placeholder = translations[key][language];
            } else {
                element.textContent = translations[key][language];
            }
        }
    });
    
    // Update page title
    const titleKey = document.body.getAttribute('data-page-title');
    if (titleKey && translations[titleKey] && translations[titleKey][language]) {
        document.title = translations[titleKey][language] + ' - SkillServe';
    }
}

// Translate text programmatically
function translateText(key, language = currentLanguage) {
    const normalizedKey = key.toLowerCase();
    if (translations[normalizedKey] && translations[normalizedKey][language]) {
        return translations[normalizedKey][language];
    }
    return key; // Return original if translation not found
}

// Get current language
function getCurrentLanguage() {
    return currentLanguage;
}

// Add translation attributes to elements
function addTranslationAttributes() {
    // This function can be called to automatically add data-translate attributes
    // to common elements based on their text content
    const commonElements = document.querySelectorAll('button, a, h1, h2, h3, label');
    
    commonElements.forEach(element => {
        const text = element.textContent.trim().toLowerCase();
        if (translations[text] && !element.hasAttribute('data-translate')) {
            element.setAttribute('data-translate', text);
        }
    });
}

// Logout confirmation functionality
function confirmLogout() {
    const message = currentLanguage === 'kannada' 
        ? 'ನೀವು ಖಚಿತವಾಗಿ ಲಾಗ್ ಔಟ್ ಮಾಡಲು ಬಯಸುವಿರಾ?' 
        : 'Are you sure you want to logout?';
    
    const confirmText = currentLanguage === 'kannada' ? 'ಹೌದು' : 'Yes';
    const cancelText = currentLanguage === 'kannada' ? 'ಇಲ್ಲ' : 'No';
    
    // Create custom confirmation modal
    const modal = document.createElement('div');
    modal.className = 'modal';
    modal.style.display = 'block';
    modal.innerHTML = `
        <div class="modal-content logout-confirmation-modal">
            <div class="logout-confirmation">
                <div class="logout-icon">🚪</div>
                <h3>${currentLanguage === 'kannada' ? 'ಲಾಗ್ ಔಟ್ ದೃಢೀಕರಣ' : 'Logout Confirmation'}</h3>
                <p>${message}</p>
                <div class="logout-actions">
                    <button class="btn btn-danger" onclick="proceedLogout()">${confirmText}</button>
                    <button class="btn btn-secondary" onclick="cancelLogout()">${cancelText}</button>
                </div>
            </div>
        </div>
    `;
    
    document.body.appendChild(modal);
    
    // Add event listener for clicking outside modal
    modal.addEventListener('click', (e) => {
        if (e.target === modal) {
            cancelLogout();
        }
    });
    
    // Store reference for cleanup
    window.currentLogoutModal = modal;
}

// Proceed with logout
function proceedLogout() {
    if (window.currentLogoutModal) {
        document.body.removeChild(window.currentLogoutModal);
        window.currentLogoutModal = null;
    }
    
    // Clear session and redirect
    clearCurrentUser();
    window.location.href = 'index.html';
}

// Cancel logout
function cancelLogout() {
    if (window.currentLogoutModal) {
        document.body.removeChild(window.currentLogoutModal);
        window.currentLogoutModal = null;
    }
}

// Override the original logout function
function logout() {
    confirmLogout();
}

// Initialize translator when DOM is loaded
document.addEventListener('DOMContentLoaded', () => {
    // Small delay to ensure other scripts have loaded
    setTimeout(() => {
        initializeTranslator();
        addTranslationAttributes();
    }, 100);
});